Auth provider a simple project which keep the record of the application and there permisitons. 

THe idea is we have lot's of desktop appliccations and we want to control who can use the applications.

The application send the api request to allow to use the applications.

application is mad by me so it wont work until it get token.

Token will get handle for each appliation our this project will just ptovide the token.

The condition is it will provide the token to only those who have regested email. so user will send the email and we will provide the token if there email is rezisted.

Our application will have an UI where we can rejisterd application and email ideas. We need full CURD.

The project should be clean no complicated logic. folow the mvc pattern. Nodejs Express vanila HTML, CSS and JS. SQLite database

Authentication is importent so must have email authentication for our UI.

simple toggle button to allow or bloack.